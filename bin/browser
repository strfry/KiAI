#!/bin/sh -x


#TODO Test for DISPLAY variable
BROWSER="links2 -g"

if [ $DISPLAY ]
 then BROWSER=uzbl
fi

URL=$1

#TODO if [shift]
if shift; then
 GET="$@"
else
 echo "Usage:$ browser URL"
 exit 1
fi

REQUEST=$URL$GET

echo $REQUEST
$BROWSER "$REQUEST"
