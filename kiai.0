#!/bin/sh -ex


===================== CLEANUP

RET=$(git status -s | wc -l)
git status

exit $RET

===================== INSTALL

cd
git init
git remote add github.kiai https://github.com/strfry/kiai/

ln -s "$PWD"/etc/skel/.* .

# "sed s/sex/sh -ex/g" -e 
